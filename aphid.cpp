using namespace std;
#include "bug.h"

//Derived class of Bug

aphid::aphid(){
	//holds position
};

int aphid::moveDirection() {
	return 0;
};

void aphid::update() {

};

/*update(){
		//..do (Pm) things.
		if (Pm) {
			int rand = math.floor(math.rand() * 8);
			switch (rand)
			{
			case(1) :
				//move up things
				break;
			case(2) :
				//move up-right things
				break;
			case(3) :
				//move right things
				break;
			case(4) :
				//move right-down things
				break;
			case(5) :
				//move down things
				break;
			case(6) :
				//move left-down things
				break;
			case(7) :
				//move left things
				break;
			case(8) :
				//move left-right things
				break;
			}
		}*/